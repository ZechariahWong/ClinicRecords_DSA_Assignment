#pragma once
#include "numQueue.h"

NumQueue::NumQueue () {
	frontNode = NULL;
	backNode = NULL;
};

bool NumQueue::isEmpty ()
{
	if (frontNode == NULL) { return true; }
	else { return false; }
}

int NumQueue::numGen () {
	
}


bool NumQueue::enqueue (Patient pt) {
	if (isEmpty) {
		
	}
}